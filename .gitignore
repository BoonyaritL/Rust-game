# 🛠 ไฟล์ build ที่สร้างขึ้นโดย cargo
target/

# ⚙️ ไฟล์สำรอง / ขยะ ที่ editor บางตัวสร้างขึ้น
*.rs.bk
*.swp
*~
.DS_Store

# 🧠 โฟลเดอร์ config ส่วนตัวจาก IDE/editor
.vscode/
.idea/

# 🏗 ไฟล์ binary ที่อาจถูก build แล้วลืมลบ
*.exe
*.out
*.bin

# 🚫 (ไม่ต้องใส่ Cargo.lock เพราะเราจะ keep มันไว้ในโปรเจกต์)
# Cargo.lock
